@typeparam TEnum

<select @oninput="OnValueChanged" disabled="@IsDisabled">
    @{ 
        foreach(TEnum enumValue in Enum.GetValues(typeof(TEnum)))
        {
            <option>@enumValue</option>
        }
    }
</select>